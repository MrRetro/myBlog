const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'myBlog',
    USERNAME: 'root',
    PASSWORD: 'root',
    PORT: '3306',
    HOST: '47.96.98.104'
  }
}

module.exports = config
