const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'myBlog',
    USERNAME: 'root',
    PASSWORD: 'root',
    PORT: '3306',
    HOST: 'xxxxxxx'
  }
}

module.exports = config
